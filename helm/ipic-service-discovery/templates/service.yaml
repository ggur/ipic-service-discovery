apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipic-service-discovery.fullname" . }}
  labels:
    {{- include "ipic-service-discovery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ipic-service-discovery.selectorLabels" . | nindent 4 }}
