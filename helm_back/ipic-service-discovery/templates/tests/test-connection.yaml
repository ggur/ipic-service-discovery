apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "left-food-api.fullname" . }}-test-connection"
  labels:
    {{- include "left-food-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "left-food-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
